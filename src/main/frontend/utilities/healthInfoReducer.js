// INITIAL STATE
const initialState = {
    username : 'Kari Nordmann',
};

// REDUCER
export default function healthInfoReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}